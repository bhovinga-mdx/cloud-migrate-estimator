# Cloud Migration Estimator

CLI tool that generates cloud migration cost/timeline estimates from sales call transcripts using Claude AI.

## Setup

```bash
uv sync
```

## Usage

```bash
uv run estimate generate <transcript_file>
uv run estimate validate-config
uv run estimate version
```
